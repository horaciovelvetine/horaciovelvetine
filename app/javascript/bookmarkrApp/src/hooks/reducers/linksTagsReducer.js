export default function linksTagsReducer(prev, test) {
  //=> should take in the value of whatever the fhuck is going to come in from test and turn that into usefule state to include as a part of the submit mutation
  console.log({prev} + {test})
  const theTagIdsArray = []
  debugger

  return theTagIdsArray
}