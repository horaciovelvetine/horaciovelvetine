

export default function settingsReducer(curState, setting) {
  const settings = curState
  
  debugger

  //? should flip the value of {setting: bool} in curState 
  //?  settings[`${setting}`] = !curState[`${setting}`]
  
  return (settings)
  }
