import type { WINDOW_COMPONENT_PROPS } from '../WindowWrapper';
export const SidebarContainer = ({ details, desktopState, isMinimized, setIsMinimized }: WINDOW_COMPONENT_PROPS) => {
  return <></>
};
