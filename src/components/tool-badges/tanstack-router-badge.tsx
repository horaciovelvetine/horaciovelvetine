import { ToolBadge } from '../site/tool-badge';

export function TanstackRouterBadge() {
	return (
		<ToolBadge
			text='Tanstack Router'
			color='red'
			url='https://tanstack.com/router/latest'
		/>
	);
}
