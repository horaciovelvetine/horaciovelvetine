import { ToolBadge } from '../site/tool-badge';

export function SpringBootBadge() {
	return (
		<ToolBadge
			text='Spring Boot'
			color='lime'
			url='https://spring.io/projects/spring-boot'
		/>
	);
}
